kind: pipeline
type: docker
name: backend

platform:
  os: linux
  arch: amd64

steps:
  - name: greeting
    image: alpine
    commands:
      - echo hello world
  - name: build
    image: maven:3-jdk-10
    commands:
      - mvn package

trigger:
  event:
    - push
    - pull_request